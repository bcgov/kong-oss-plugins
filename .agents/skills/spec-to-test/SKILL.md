---
name: spec-to-test
description: Generate clean-room automated tests (Playwright integration + busted unit/schema) for a Kong plugin from its OpenSpec spec. Use when asked to generate tests for a plugin from its spec, run spec-to-test, or implement spec-driven plugin tests.
---

This is a Codex adapter for the repository's canonical spec-to-test workflow.

Before taking any task action, read
[the canonical spec-to-test instructions](../../../.claude/skills/spec-to-test/SKILL.md)
completely and follow them as authoritative.

Use the plugin named in the user's request as the workflow input.
